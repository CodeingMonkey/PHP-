当对象方式调用不存在的或者受保护的方法时，__call方法会被调用(__call方式也可以访问静态方法)
当类方式调用不存在的或者受保护的静态方法的时候，__callStatic方法会被调用(
__callStatic方法比较复杂，当对象方式调用方法的时候，会走正常逻辑，不会经过__callStatic方法。
当类方式调用受保护的方法、不存在的方法的时候，会走__callStatic。
当类方式调用存在的public的方法的时候，会直接调用不经过__callStatic)

__call和__callStatic方法作用不仅能够防止调用不存在或者不能访问的方法报错，能够使方法的动态创建成为可能。
