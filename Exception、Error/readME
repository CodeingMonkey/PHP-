测试当前whoops组件和monolog组件时，请先执行composer install

关于日志最后有[]的解决方案：https://cloud.tencent.com/developer/ask/83307



PHP常见的错误级别：
deprecated：不建议、不推荐。程序中使用了不推荐的语法的时候，就会报出这个级别的错误，不影响程序运行。
notice：语法中存在不当的代码就会报出当前错误（使用未定义的变量就会报出这个错误），程序可以继续运行。
warning：较高级别错误（调用方法时，方法参数不匹配就会抛出这个级别的错误），程序依然会继续运行。
fetal_error：致命错误，程序会立即终止。
prase_error：语法错误（这种情况一般不会出现，编辑器遇到这种错误都会直接提示）