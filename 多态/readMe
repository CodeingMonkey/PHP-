多态：同一类型对象收到相同的消息时，会有不同的结果。

判断是否是多态：
对象是否属于同一类型，如果属于同一类型，调用相同函数，返回了不同结果。
多态和业务无关，不能根据一个方法收到同一类对象只是因为逻辑处理不同得到不同结果就是多态。
多态调用的方法其实是不同的方法，如果走的是同一个方法那就不是多态。（TestInterface中就是走到了不同类的方法体，虽然方法名相同）

总结：
多态是同一类对象在运行时的具体化。
PHP是弱类型的实现多态更方便（接口就是一个很明显的多态实现）。
类型转换不是多态。
PHP中父类和子类是继承关系，两者在亲属关系上并不是真正的血缘关系，所以子类无法向上转型为父类，所以算不上多态。
多态的本质就是if...else。

